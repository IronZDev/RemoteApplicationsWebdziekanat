'use strict';

describe('myApp.submitApplicationPage module', function() {

    beforeEach(module('myApp.submitApplicationPage'));

    describe('submitApplicationPage controller', function(){

        it('should ....', inject(function($controller) {
            //spec body
            var submitApplicationPageController = $controller('submitApplicationPageController');
            expect(submitApplicationPageController).toBeDefined();
        }));

    });
});