'use strict';

describe('myApp.submitAppealPage module', function() {

    beforeEach(module('myApp.submitAppealPage'));

    describe('submitAppealPage controller', function(){

        it('should ....', inject(function($controller) {
            //spec body
            var submitAppealPageController = $controller('submitAppealPageController');
            expect(submitAppealPageController).toBeDefined();
        }));

    });
});