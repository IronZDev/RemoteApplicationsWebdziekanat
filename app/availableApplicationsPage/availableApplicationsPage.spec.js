'use strict';

describe('myApp.availableApplicationsPage module', function() {

    beforeEach(module('myApp.availableApplicationsPage'));

    describe('availableApplicationsPage controller', function(){

        it('should ....', inject(function($controller) {
            //spec body
            var availableApplicationsPageController = $controller('availableApplicationsPageController');
            expect(availableApplicationsPageController).toBeDefined();
        }));

    });
});