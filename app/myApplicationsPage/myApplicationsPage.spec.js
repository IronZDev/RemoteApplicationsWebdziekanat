'use strict';

describe('myApp.myApplicationsPage module', function() {

    beforeEach(module('myApp.myApplicationsPage'));

    describe('myApplicationsPage controller', function(){

        it('should ....', inject(function($controller) {
            //spec body
            var myApplicationsPageController = $controller('myApplicationsPageController');
            expect(myApplicationsPageController).toBeDefined();
        }));

    });
});